//// [tests/cases/compiler/SwitchExpression.ts] ////

=== SwitchExpression.ts ===
{
  const a: boolean = true;
>a : Symbol(a, Decl(SwitchExpression.ts, 1, 7))

  const r = switch (a) {
>r : Symbol(r, Decl(SwitchExpression.ts, 2, 7))
>a : Symbol(a, Decl(SwitchExpression.ts, 2, 20))
>a : Symbol(a, Decl(SwitchExpression.ts, 1, 7))
>a : Symbol(a, Decl(SwitchExpression.ts, 2, 20))

    case true => "yes";
>case true => : Symbol(case true =>, Decl(SwitchExpression.ts, 2, 24))

    case _ => "no";
>_ : Symbol(_, Decl(SwitchExpression.ts, 4, 9))


>a : Symbol(a, Decl(SwitchExpression.ts, 2, 20))

    case true => "yes";
    case _ => "no";
  };
}

{
  const a: boolean = true;
>a : Symbol(a, Decl(SwitchExpression.ts, 9, 7))

  const r = switch (a) { case true => "yes" case _ => "no" };
>r : Symbol(r, Decl(SwitchExpression.ts, 10, 7))
>a : Symbol(a, Decl(SwitchExpression.ts, 10, 20))
>a : Symbol(a, Decl(SwitchExpression.ts, 9, 7))
>a : Symbol(a, Decl(SwitchExpression.ts, 10, 20))
>case true => : Symbol(case true =>, Decl(SwitchExpression.ts, 10, 24))
>_ : Symbol(_, Decl(SwitchExpression.ts, 10, 49))
>a : Symbol(a, Decl(SwitchExpression.ts, 10, 20))
}

{
  const a: number = 1;
>a : Symbol(a, Decl(SwitchExpression.ts, 14, 7))

  const r = switch (a) {
>r : Symbol(r, Decl(SwitchExpression.ts, 15, 7))
>a : Symbol(a, Decl(SwitchExpression.ts, 15, 20))
>a : Symbol(a, Decl(SwitchExpression.ts, 14, 7))
>a : Symbol(a, Decl(SwitchExpression.ts, 15, 20))

    case 0 => "zero";
>case 0 => : Symbol(case 0 =>, Decl(SwitchExpression.ts, 15, 24))


>a : Symbol(a, Decl(SwitchExpression.ts, 15, 20))

    case 0 => "zero";
    case 1 => "one";
>case 1 => : Symbol(case 1 =>, Decl(SwitchExpression.ts, 16, 21))

    case _ => "many";
>_ : Symbol(_, Decl(SwitchExpression.ts, 18, 9))


>a : Symbol(a, Decl(SwitchExpression.ts, 15, 20))

    case 0 => "zero";
    case 1 => "one";
    case _ => "many";
  };
}

{
  const a: string = "hoge";
>a : Symbol(a, Decl(SwitchExpression.ts, 23, 7))

  const r = switch (a) {
>r : Symbol(r, Decl(SwitchExpression.ts, 24, 7))
>a : Symbol(a, Decl(SwitchExpression.ts, 24, 20))
>a : Symbol(a, Decl(SwitchExpression.ts, 23, 7))
>a : Symbol(a, Decl(SwitchExpression.ts, 24, 20))

    case "fuga" => "string fuga";
>case "fuga" => : Symbol(case "fuga" =>, Decl(SwitchExpression.ts, 24, 24))


>a : Symbol(a, Decl(SwitchExpression.ts, 24, 20))

    case "fuga" => "string fuga";
    case "hoge" => "string hoge";
>case "hoge" => : Symbol(case "hoge" =>, Decl(SwitchExpression.ts, 25, 33))

    case _ => "string unknown";
>_ : Symbol(_, Decl(SwitchExpression.ts, 27, 9))


>a : Symbol(a, Decl(SwitchExpression.ts, 24, 20))

    case "fuga" => "string fuga";
    case "hoge" => "string hoge";
    case _ => "string unknown";
  };
}

{
  const a: number | null | undefined = 1;
>a : Symbol(a, Decl(SwitchExpression.ts, 32, 7))

  const r = switch (a) {
>r : Symbol(r, Decl(SwitchExpression.ts, 33, 7))
>a : Symbol(a, Decl(SwitchExpression.ts, 33, 20))
>a : Symbol(a, Decl(SwitchExpression.ts, 32, 7))
>a : Symbol(a, Decl(SwitchExpression.ts, 33, 20))

    case null => "null";
>case null => : Symbol(case null =>, Decl(SwitchExpression.ts, 33, 24))


>a : Symbol(a, Decl(SwitchExpression.ts, 33, 20))

    case null => "null";
    case undefined => "undefined";
>case undefined => : Symbol(case undefined =>, Decl(SwitchExpression.ts, 34, 24))

    case _ => "number";
>_ : Symbol(_, Decl(SwitchExpression.ts, 36, 9))


>a : Symbol(a, Decl(SwitchExpression.ts, 33, 20))

    case null => "null";
    case undefined => "undefined";
    case _ => "number";
  };
}

{
  const elem: unknown[] = [];
>elem : Symbol(elem, Decl(SwitchExpression.ts, 41, 7))

  const r = switch (elem) {
>r : Symbol(r, Decl(SwitchExpression.ts, 42, 7))
>elem : Symbol(elem, Decl(SwitchExpression.ts, 42, 20))
>elem : Symbol(elem, Decl(SwitchExpression.ts, 41, 7))
>elem : Symbol(elem, Decl(SwitchExpression.ts, 42, 20))
>elem : Symbol(elem, Decl(SwitchExpression.ts, 42, 20))

    case [true] => "[true]";
>case [true] => : Symbol(case [true] =>, Decl(SwitchExpression.ts, 42, 27))


>elem : Symbol(elem, Decl(SwitchExpression.ts, 42, 20))
>elem : Symbol(elem, Decl(SwitchExpression.ts, 42, 20))

    case [true] => "[true]";
    case [1] => "[1]";
>case [1] => : Symbol(case [1] =>, Decl(SwitchExpression.ts, 43, 28))


>elem : Symbol(elem, Decl(SwitchExpression.ts, 42, 20))
>elem : Symbol(elem, Decl(SwitchExpression.ts, 42, 20))

    case [true] => "[true]";
    case [1] => "[1]";
    case ["hoge"] => "[hoge]";
>case ["hoge"] => : Symbol(case ["hoge"] =>, Decl(SwitchExpression.ts, 44, 22))


>elem : Symbol(elem, Decl(SwitchExpression.ts, 42, 20))
>elem : Symbol(elem, Decl(SwitchExpression.ts, 42, 20))

    case [true] => "[true]";
    case [1] => "[1]";
    case ["hoge"] => "[hoge]";
    case [null] => "[null]";
>case [null] => : Symbol(case [null] =>, Decl(SwitchExpression.ts, 45, 30))


>elem : Symbol(elem, Decl(SwitchExpression.ts, 42, 20))
>elem : Symbol(elem, Decl(SwitchExpression.ts, 42, 20))

    case [true] => "[true]";
    case [1] => "[1]";
    case ["hoge"] => "[hoge]";
    case [null] => "[null]";
    case [undefined] => "[undefined]";
>case [undefined] => : Symbol(case [undefined] =>, Decl(SwitchExpression.ts, 46, 28))


> : Symbol((Missing), Decl(SwitchExpression.ts, 0, 0))

  const a: boolean = true;
  const r = switch (a) {
    case true => "yes";
    case _ => "no";
  };
}

{
  const a: boolean = true;
  const r = switch (a) { case true => "yes" case _ => "no" };
}

{
  const a: number = 1;
  const r = switch (a) {
    case 0 => "zero";
    case 1 => "one";
    case _ => "many";
  };
}

{
  const a: string = "hoge";
  const r = switch (a) {
    case "fuga" => "string fuga";
    case "hoge" => "string hoge";
    case _ => "string unknown";
  };
}

{
  const a: number | null | undefined = 1;
  const r = switch (a) {
    case null => "null";
    case undefined => "undefined";
    case _ => "number";
  };
}

{
  const elem: unknown[] = [];
  const r = switch (elem) {
>elem : Symbol(elem, Decl(SwitchExpression.ts, 42, 20))
>elem : Symbol(elem, Decl(SwitchExpression.ts, 42, 20))

    case [true] => "[true]";
    case [1] => "[1]";
    case ["hoge"] => "[hoge]";
    case [null] => "[null]";
    case [undefined] => "[undefined]";
    case [elem] => "[elem]";
>case [elem] => : Symbol(case [elem] =>, Decl(SwitchExpression.ts, 47, 38))


> : Symbol((Missing), Decl(SwitchExpression.ts, 0, 0))

  const a: boolean = true;
  const r = switch (a) {
    case true => "yes";
    case _ => "no";
  };
}

{
  const a: boolean = true;
  const r = switch (a) { case true => "yes" case _ => "no" };
}

{
  const a: number = 1;
  const r = switch (a) {
    case 0 => "zero";
    case 1 => "one";
    case _ => "many";
  };
}

{
  const a: string = "hoge";
  const r = switch (a) {
    case "fuga" => "string fuga";
    case "hoge" => "string hoge";
    case _ => "string unknown";
  };
}

{
  const a: number | null | undefined = 1;
  const r = switch (a) {
    case null => "null";
    case undefined => "undefined";
    case _ => "number";
  };
}

{
  const elem: unknown[] = [];
  const r = switch (elem) {
>elem : Symbol(elem, Decl(SwitchExpression.ts, 42, 20))
>elem : Symbol(elem, Decl(SwitchExpression.ts, 42, 20))

    case [true] => "[true]";
    case [1] => "[1]";
    case ["hoge"] => "[hoge]";
    case [null] => "[null]";
    case [undefined] => "[undefined]";
    case [elem] => "[elem]";
    case [...tail] => "[...tail]";
>case [...tail] => : Symbol(case [...tail] =>, Decl(SwitchExpression.ts, 48, 28))


> : Symbol((Missing), Decl(SwitchExpression.ts, 0, 0))

  const a: boolean = true;
  const r = switch (a) {
    case true => "yes";
    case _ => "no";
  };
}

{
  const a: boolean = true;
  const r = switch (a) { case true => "yes" case _ => "no" };
}

{
  const a: number = 1;
  const r = switch (a) {
    case 0 => "zero";
    case 1 => "one";
    case _ => "many";
  };
}

{
  const a: string = "hoge";
  const r = switch (a) {
    case "fuga" => "string fuga";
    case "hoge" => "string hoge";
    case _ => "string unknown";
  };
}

{
  const a: number | null | undefined = 1;
  const r = switch (a) {
    case null => "null";
    case undefined => "undefined";
    case _ => "number";
  };
}

{
  const elem: unknown[] = [];
  const r = switch (elem) {
>elem : Symbol(elem, Decl(SwitchExpression.ts, 42, 20))


> : Symbol((Missing), Decl(SwitchExpression.ts, 0, 0))

  const a: boolean = true;
  const r = switch (a) {
    case true => "yes";
    case _ => "no";
  };
}

{
  const a: boolean = true;
  const r = switch (a) { case true => "yes" case _ => "no" };
}

{
  const a: number = 1;
  const r = switch (a) {
    case 0 => "zero";
    case 1 => "one";
    case _ => "many";
  };
}

{
  const a: string = "hoge";
  const r = switch (a) {
    case "fuga" => "string fuga";
    case "hoge" => "string hoge";
    case _ => "string unknown";
  };
}

{
  const a: number | null | undefined = 1;
  const r = switch (a) {
    case null => "null";
    case undefined => "undefined";
    case _ => "number";
  };
}

{
  const elem: unknown[] = [];
  const r = switch (elem) {
>elem : Symbol(elem, Decl(SwitchExpression.ts, 42, 20))
>elem : Symbol(elem, Decl(SwitchExpression.ts, 42, 20))

    case [true] => "[true]";
    case [1] => "[1]";
    case ["hoge"] => "[hoge]";
    case [null] => "[null]";
    case [undefined] => "[undefined]";
    case [elem] => "[elem]";
    case [...tail] => "[...tail]";
    case [x,] => "[x,]";
>case [x,] => : Symbol(case [x,] =>, Decl(SwitchExpression.ts, 49, 34))


> : Symbol((Missing), Decl(SwitchExpression.ts, 0, 0))

  const a: boolean = true;
  const r = switch (a) {
    case true => "yes";
    case _ => "no";
  };
}

{
  const a: boolean = true;
  const r = switch (a) { case true => "yes" case _ => "no" };
}

{
  const a: number = 1;
  const r = switch (a) {
    case 0 => "zero";
    case 1 => "one";
    case _ => "many";
  };
}

{
  const a: string = "hoge";
  const r = switch (a) {
    case "fuga" => "string fuga";
    case "hoge" => "string hoge";
    case _ => "string unknown";
  };
}

{
  const a: number | null | undefined = 1;
  const r = switch (a) {
    case null => "null";
    case undefined => "undefined";
    case _ => "number";
  };
}

{
  const elem: unknown[] = [];
  const r = switch (elem) {
>elem : Symbol(elem, Decl(SwitchExpression.ts, 42, 20))
>elem : Symbol(elem, Decl(SwitchExpression.ts, 42, 20))
>elem : Symbol(elem, Decl(SwitchExpression.ts, 42, 20))

    case [true] => "[true]";
    case [1] => "[1]";
    case ["hoge"] => "[hoge]";
    case [null] => "[null]";
    case [undefined] => "[undefined]";
    case [elem] => "[elem]";
    case [...tail] => "[...tail]";
    case [x,] => "[x,]";
    case [x, 1] => "[x, 1]";
>case [x, 1] => : Symbol(case [x, 1] =>, Decl(SwitchExpression.ts, 50, 24))


> : Symbol((Missing), Decl(SwitchExpression.ts, 0, 0))

  const a: boolean = true;
  const r = switch (a) {
    case true => "yes";
    case _ => "no";
  };
}

{
  const a: boolean = true;
  const r = switch (a) { case true => "yes" case _ => "no" };
}

{
  const a: number = 1;
  const r = switch (a) {
    case 0 => "zero";
    case 1 => "one";
    case _ => "many";
  };
}

{
  const a: string = "hoge";
  const r = switch (a) {
    case "fuga" => "string fuga";
    case "hoge" => "string hoge";
    case _ => "string unknown";
  };
}

{
  const a: number | null | undefined = 1;
  const r = switch (a) {
    case null => "null";
    case undefined => "undefined";
    case _ => "number";
  };
}

{
  const elem: unknown[] = [];
  const r = switch (elem) {
>elem : Symbol(elem, Decl(SwitchExpression.ts, 42, 20))
>elem : Symbol(elem, Decl(SwitchExpression.ts, 42, 20))


> : Symbol((Missing), Decl(SwitchExpression.ts, 0, 0))

  const a: boolean = true;
  const r = switch (a) {
    case true => "yes";
    case _ => "no";
  };
}

{
  const a: boolean = true;
  const r = switch (a) { case true => "yes" case _ => "no" };
}

{
  const a: number = 1;
  const r = switch (a) {
    case 0 => "zero";
    case 1 => "one";
    case _ => "many";
  };
}

{
  const a: string = "hoge";
  const r = switch (a) {
    case "fuga" => "string fuga";
    case "hoge" => "string hoge";
    case _ => "string unknown";
  };
}

{
  const a: number | null | undefined = 1;
  const r = switch (a) {
    case null => "null";
    case undefined => "undefined";
    case _ => "number";
  };
}

{
  const elem: unknown[] = [];
  const r = switch (elem) {
>elem : Symbol(elem, Decl(SwitchExpression.ts, 42, 20))
>elem : Symbol(elem, Decl(SwitchExpression.ts, 42, 20))

    case [true] => "[true]";
    case [1] => "[1]";
    case ["hoge"] => "[hoge]";
    case [null] => "[null]";
    case [undefined] => "[undefined]";
    case [elem] => "[elem]";
    case [...tail] => "[...tail]";
    case [x,] => "[x,]";
    case [x, 1] => "[x, 1]";
    case [x, 2, ...tail] => "[x, 2, ...tail]";
>case [x, 2, ...tail] => : Symbol(case [x, 2, ...tail] =>, Decl(SwitchExpression.ts, 51, 28))


>elem : Symbol(elem, Decl(SwitchExpression.ts, 42, 20))


> : Symbol((Missing), Decl(SwitchExpression.ts, 0, 0))

  const a: boolean = true;
  const r = switch (a) {
    case true => "yes";
    case _ => "no";
  };
}

{
  const a: boolean = true;
  const r = switch (a) { case true => "yes" case _ => "no" };
}

{
  const a: number = 1;
  const r = switch (a) {
    case 0 => "zero";
    case 1 => "one";
    case _ => "many";
  };
}

{
  const a: string = "hoge";
  const r = switch (a) {
    case "fuga" => "string fuga";
    case "hoge" => "string hoge";
    case _ => "string unknown";
  };
}

{
  const a: number | null | undefined = 1;
  const r = switch (a) {
    case null => "null";
    case undefined => "undefined";
    case _ => "number";
  };
}

{
  const elem: unknown[] = [];
  const r = switch (elem) {
>elem : Symbol(elem, Decl(SwitchExpression.ts, 42, 20))


> : Symbol((Missing), Decl(SwitchExpression.ts, 0, 0))
> : Symbol((Missing), Decl(SwitchExpression.ts, 0, 0))

  const a: boolean = true;
  const r = switch (a) {
    case true => "yes";
    case _ => "no";
  };
}

{
  const a: boolean = true;
  const r = switch (a) { case true => "yes" case _ => "no" };
}

{
  const a: number = 1;
  const r = switch (a) {
    case 0 => "zero";
    case 1 => "one";
    case _ => "many";
  };
}

{
  const a: string = "hoge";
  const r = switch (a) {
    case "fuga" => "string fuga";
    case "hoge" => "string hoge";
    case _ => "string unknown";
  };
}

{
  const a: number | null | undefined = 1;
  const r = switch (a) {
    case null => "null";
    case undefined => "undefined";
    case _ => "number";
  };
}

{
  const elem: unknown[] = [];
  const r = switch (elem) {
>elem : Symbol(elem, Decl(SwitchExpression.ts, 42, 20))
>elem : Symbol(elem, Decl(SwitchExpression.ts, 42, 20))

    case [true] => "[true]";
    case [1] => "[1]";
    case ["hoge"] => "[hoge]";
    case [null] => "[null]";
    case [undefined] => "[undefined]";
    case [elem] => "[elem]";
    case [...tail] => "[...tail]";
    case [x,] => "[x,]";
    case [x, 1] => "[x, 1]";
    case [x, 2, ...tail] => "[x, 2, ...tail]";
    case [{x: 1, y}, [z], ...tail] => "[{x: 1, y}, [z], ...tail]";
>case [{x: 1, y}, [z], ...tail] => : Symbol(case [{x: 1, y}, [z], ...tail] =>, Decl(SwitchExpression.ts, 52, 46))

  };
}

{
  const obj: Record<string, unknown> = {};
>obj : Symbol(obj, Decl(SwitchExpression.ts, 58, 7))
>Record : Symbol(Record, Decl(lib.es5.d.ts, --, --))

  const r = switch (obj) {
>r : Symbol(r, Decl(SwitchExpression.ts, 59, 7))
>obj : Symbol(obj, Decl(SwitchExpression.ts, 59, 20))
>obj : Symbol(obj, Decl(SwitchExpression.ts, 58, 7))


> : Symbol((Missing), Decl(SwitchExpression.ts, 0, 0))

  const a: boolean = true;
  const r = switch (a) {
    case true => "yes";
    case _ => "no";
  };
}

{
  const a: boolean = true;
  const r = switch (a) { case true => "yes" case _ => "no" };
}

{
  const a: number = 1;
  const r = switch (a) {
    case 0 => "zero";
    case 1 => "one";
    case _ => "many";
  };
}

{
  const a: string = "hoge";
  const r = switch (a) {
    case "fuga" => "string fuga";
    case "hoge" => "string hoge";
    case _ => "string unknown";
  };
}

{
  const a: number | null | undefined = 1;
  const r = switch (a) {
    case null => "null";
    case undefined => "undefined";
    case _ => "number";
  };
}

{
  const elem: unknown[] = [];
  const r = switch (elem) {
    case [true] => "[true]";
    case [1] => "[1]";
    case ["hoge"] => "[hoge]";
    case [null] => "[null]";
    case [undefined] => "[undefined]";
    case [elem] => "[elem]";
    case [...tail] => "[...tail]";
    case [x,] => "[x,]";
    case [x, 1] => "[x, 1]";
    case [x, 2, ...tail] => "[x, 2, ...tail]";
    case [{x: 1, y}, [z], ...tail] => "[{x: 1, y}, [z], ...tail]";
  };
}

{
  const obj: Record<string, unknown> = {};
  const r = switch (obj) {
>obj : Symbol(obj, Decl(SwitchExpression.ts, 59, 20))
>obj : Symbol(obj, Decl(SwitchExpression.ts, 59, 20))
>obj : Symbol(obj, Decl(SwitchExpression.ts, 59, 20))

    case { x } => "{ x }";
>case { x } => : Symbol(case { x } =>, Decl(SwitchExpression.ts, 59, 26))


>obj : Symbol(obj, Decl(SwitchExpression.ts, 59, 20))
>obj : Symbol(obj, Decl(SwitchExpression.ts, 59, 20))
>obj : Symbol(obj, Decl(SwitchExpression.ts, 59, 20))

    case { x } => "{ x }";
    case { x: false } => "{ x: false }";
>case { x: false } => : Symbol(case { x: false } =>, Decl(SwitchExpression.ts, 60, 26))


>obj : Symbol(obj, Decl(SwitchExpression.ts, 59, 20))
>obj : Symbol(obj, Decl(SwitchExpression.ts, 59, 20))
>obj : Symbol(obj, Decl(SwitchExpression.ts, 59, 20))

    case { x } => "{ x }";
    case { x: false } => "{ x: false }";
    case { x: 1 } => "{ x: 1 }";
>case { x: 1 } => : Symbol(case { x: 1 } =>, Decl(SwitchExpression.ts, 61, 40))


>obj : Symbol(obj, Decl(SwitchExpression.ts, 59, 20))
>obj : Symbol(obj, Decl(SwitchExpression.ts, 59, 20))
>obj : Symbol(obj, Decl(SwitchExpression.ts, 59, 20))

    case { x } => "{ x }";
    case { x: false } => "{ x: false }";
    case { x: 1 } => "{ x: 1 }";
    case { "x": 1 } => "{ 'x': 1 }";
>case { "x": 1 } => : Symbol(case { "x": 1 } =>, Decl(SwitchExpression.ts, 62, 32))


>obj : Symbol(obj, Decl(SwitchExpression.ts, 59, 20))

    case { x } => "{ x }";
    case { x: false } => "{ x: false }";
    case { x: 1 } => "{ x: 1 }";
    case { "x": 1 } => "{ 'x': 1 }";
    case { "x": a } => "{ 'x': a }";
>a : Symbol(a, Decl(SwitchExpression.ts, 64, 16))


>obj : Symbol(obj, Decl(SwitchExpression.ts, 59, 20))
>obj : Symbol(obj, Decl(SwitchExpression.ts, 59, 20))

    case { x } => "{ x }";
    case { x: false } => "{ x: false }";
    case { x: 1 } => "{ x: 1 }";
    case { "x": 1 } => "{ 'x': 1 }";
    case { "x": a } => "{ 'x': a }";
>case { "x": a } => : Symbol(case { "x": a } =>, Decl(SwitchExpression.ts, 63, 36))

    case { ...tail } => "{ ...tail }";
>tail : Symbol(tail, Decl(SwitchExpression.ts, 0, 0))


>obj : Symbol(obj, Decl(SwitchExpression.ts, 59, 20))
>obj : Symbol(obj, Decl(SwitchExpression.ts, 59, 20))
>obj : Symbol(obj, Decl(SwitchExpression.ts, 59, 20))

    case { x } => "{ x }";
    case { x: false } => "{ x: false }";
    case { x: 1 } => "{ x: 1 }";
    case { "x": 1 } => "{ 'x': 1 }";
    case { "x": a } => "{ 'x': a }";
    case { ...tail } => "{ ...tail }";
>case { ...tail } => : Symbol(case { ...tail } =>, Decl(SwitchExpression.ts, 64, 36))


>obj : Symbol(obj, Decl(SwitchExpression.ts, 59, 20))
>obj : Symbol(obj, Decl(SwitchExpression.ts, 59, 20))


> : Symbol((Missing), Decl(SwitchExpression.ts, 0, 0))

  const a: boolean = true;
  const r = switch (a) {
    case true => "yes";
    case _ => "no";
  };
}

{
  const a: boolean = true;
  const r = switch (a) { case true => "yes" case _ => "no" };
}

{
  const a: number = 1;
  const r = switch (a) {
    case 0 => "zero";
    case 1 => "one";
    case _ => "many";
  };
}

{
  const a: string = "hoge";
  const r = switch (a) {
    case "fuga" => "string fuga";
    case "hoge" => "string hoge";
    case _ => "string unknown";
  };
}

{
  const a: number | null | undefined = 1;
  const r = switch (a) {
    case null => "null";
    case undefined => "undefined";
    case _ => "number";
  };
}

{
  const elem: unknown[] = [];
  const r = switch (elem) {
    case [true] => "[true]";
    case [1] => "[1]";
    case ["hoge"] => "[hoge]";
    case [null] => "[null]";
    case [undefined] => "[undefined]";
    case [elem] => "[elem]";
    case [...tail] => "[...tail]";
    case [x,] => "[x,]";
    case [x, 1] => "[x, 1]";
    case [x, 2, ...tail] => "[x, 2, ...tail]";
    case [{x: 1, y}, [z], ...tail] => "[{x: 1, y}, [z], ...tail]";
  };
}

{
  const obj: Record<string, unknown> = {};
  const r = switch (obj) {
>obj : Symbol(obj, Decl(SwitchExpression.ts, 59, 20))
>obj : Symbol(obj, Decl(SwitchExpression.ts, 59, 20))
>obj : Symbol(obj, Decl(SwitchExpression.ts, 59, 20))

    case { x } => "{ x }";
    case { x: false } => "{ x: false }";
    case { x: 1 } => "{ x: 1 }";
    case { "x": 1 } => "{ 'x': 1 }";
    case { "x": a } => "{ 'x': a }";
    case { ...tail } => "{ ...tail }";
    case { x: [1, 2], y: { a: "a", b } } => "{ x: [1, 2] }";
>case { x: [1, 2], y: { a: "a", b } } => : Symbol(case { x: [1, 2], y: { a: "a", b } } =>, Decl(SwitchExpression.ts, 65, 38))


>obj : Symbol(obj, Decl(SwitchExpression.ts, 59, 20))


> : Symbol((Missing), Decl(SwitchExpression.ts, 0, 0))

  const a: boolean = true;
  const r = switch (a) {
    case true => "yes";
    case _ => "no";
  };
}

{
  const a: boolean = true;
  const r = switch (a) { case true => "yes" case _ => "no" };
}

{
  const a: number = 1;
  const r = switch (a) {
    case 0 => "zero";
    case 1 => "one";
    case _ => "many";
  };
}

{
  const a: string = "hoge";
  const r = switch (a) {
    case "fuga" => "string fuga";
    case "hoge" => "string hoge";
    case _ => "string unknown";
  };
}

{
  const a: number | null | undefined = 1;
  const r = switch (a) {
    case null => "null";
    case undefined => "undefined";
    case _ => "number";
  };
}

{
  const elem: unknown[] = [];
  const r = switch (elem) {
    case [true] => "[true]";
    case [1] => "[1]";
    case ["hoge"] => "[hoge]";
    case [null] => "[null]";
    case [undefined] => "[undefined]";
    case [elem] => "[elem]";
    case [...tail] => "[...tail]";
    case [x,] => "[x,]";
    case [x, 1] => "[x, 1]";
    case [x, 2, ...tail] => "[x, 2, ...tail]";
    case [{x: 1, y}, [z], ...tail] => "[{x: 1, y}, [z], ...tail]";
  };
}

{
  const obj: Record<string, unknown> = {};
  const r = switch (obj) {
>obj : Symbol(obj, Decl(SwitchExpression.ts, 59, 20))

    case { x } => "{ x }";
    case { x: false } => "{ x: false }";
    case { x: 1 } => "{ x: 1 }";
    case { "x": 1 } => "{ 'x': 1 }";
    case { "x": a } => "{ 'x': a }";
    case { ...tail } => "{ ...tail }";
    case { x: [1, 2], y: { a: "a", b } } => "{ x: [1, 2] }";
    case { x: 1, y, ...tail } => "{ x: 1, y, ...tail }";
>tail : Symbol(tail, Decl(SwitchExpression.ts, 0, 0))


>obj : Symbol(obj, Decl(SwitchExpression.ts, 59, 20))


> : Symbol(Omit)

  const a: boolean = true;
  const r = switch (a) {
    case true => "yes";
    case _ => "no";
  };
}

{
  const a: boolean = true;
  const r = switch (a) { case true => "yes" case _ => "no" };
}

{
  const a: number = 1;
  const r = switch (a) {
    case 0 => "zero";
    case 1 => "one";
    case _ => "many";
  };
}

{
  const a: string = "hoge";
  const r = switch (a) {
    case "fuga" => "string fuga";
    case "hoge" => "string hoge";
    case _ => "string unknown";
  };
}

{
  const a: number | null | undefined = 1;
  const r = switch (a) {
    case null => "null";
    case undefined => "undefined";
    case _ => "number";
  };
}

{
  const elem: unknown[] = [];
  const r = switch (elem) {
    case [true] => "[true]";
    case [1] => "[1]";
    case ["hoge"] => "[hoge]";
    case [null] => "[null]";
    case [undefined] => "[undefined]";
    case [elem] => "[elem]";
    case [...tail] => "[...tail]";
    case [x,] => "[x,]";
    case [x, 1] => "[x, 1]";
    case [x, 2, ...tail] => "[x, 2, ...tail]";
    case [{x: 1, y}, [z], ...tail] => "[{x: 1, y}, [z], ...tail]";
  };
}

{
  const obj: Record<string, unknown> = {};
  const r = switch (obj) {
>obj : Symbol(obj, Decl(SwitchExpression.ts, 59, 20))
>obj : Symbol(obj, Decl(SwitchExpression.ts, 59, 20))
>obj : Symbol(obj, Decl(SwitchExpression.ts, 59, 20))
>obj : Symbol(obj, Decl(SwitchExpression.ts, 59, 20))

    case { x } => "{ x }";
    case { x: false } => "{ x: false }";
    case { x: 1 } => "{ x: 1 }";
    case { "x": 1 } => "{ 'x': 1 }";
    case { "x": a } => "{ 'x': a }";
    case { ...tail } => "{ ...tail }";
    case { x: [1, 2], y: { a: "a", b } } => "{ x: [1, 2] }";
    case { x: 1, y, ...tail } => "{ x: 1, y, ...tail }";
>case { x: 1, y, ...tail } => : Symbol(case { x: 1, y, ...tail } =>, Decl(SwitchExpression.ts, 66, 60))

    case _ if _.x === 1 => "_ if _.x === 1";
>_ : Symbol(_, Decl(SwitchExpression.ts, 68, 9))


>obj : Symbol(obj, Decl(SwitchExpression.ts, 59, 20))

    case { x } => "{ x }";
    case { x: false } => "{ x: false }";
    case { x: 1 } => "{ x: 1 }";
    case { "x": 1 } => "{ 'x': 1 }";
    case { "x": a } => "{ 'x': a }";
    case { ...tail } => "{ ...tail }";
    case { x: [1, 2], y: { a: "a", b } } => "{ x: [1, 2] }";
    case { x: 1, y, ...tail } => "{ x: 1, y, ...tail }";
    case _ if _.x === 1 => "_ if _.x === 1";
>_ : Symbol(_, Decl(SwitchExpression.ts, 68, 9))
>case _ if _.x === 1 => : Symbol(case _ if _.x === 1 =>, Decl(SwitchExpression.ts, 67, 56))

    case _ => "_";
>_ : Symbol(_, Decl(SwitchExpression.ts, 69, 9))


>obj : Symbol(obj, Decl(SwitchExpression.ts, 59, 20))

    case { x } => "{ x }";
    case { x: false } => "{ x: false }";
    case { x: 1 } => "{ x: 1 }";
    case { "x": 1 } => "{ 'x': 1 }";
    case { "x": a } => "{ 'x': a }";
    case { ...tail } => "{ ...tail }";
    case { x: [1, 2], y: { a: "a", b } } => "{ x: [1, 2] }";
    case { x: 1, y, ...tail } => "{ x: 1, y, ...tail }";
    case _ if _.x === 1 => "_ if _.x === 1";
    case _ => "_";
  };
}

{
  const a = { x: 1, y: 2, z: 3 };
>a : Symbol(a, Decl(SwitchExpression.ts, 74, 7))
>x : Symbol(x, Decl(SwitchExpression.ts, 74, 13))
>y : Symbol(y, Decl(SwitchExpression.ts, 74, 19))
>z : Symbol(z, Decl(SwitchExpression.ts, 74, 25))

  const r = switch (a) {
>r : Symbol(r, Decl(SwitchExpression.ts, 75, 7))
>a : Symbol(a, Decl(SwitchExpression.ts, 75, 20))
>a : Symbol(a, Decl(SwitchExpression.ts, 74, 7))


> : Symbol((Missing), Decl(SwitchExpression.ts, 0, 0))

  const a: boolean = true;
  const r = switch (a) {
    case true => "yes";
    case _ => "no";
  };
}

{
  const a: boolean = true;
  const r = switch (a) { case true => "yes" case _ => "no" };
}

{
  const a: number = 1;
  const r = switch (a) {
    case 0 => "zero";
    case 1 => "one";
    case _ => "many";
  };
}

{
  const a: string = "hoge";
  const r = switch (a) {
    case "fuga" => "string fuga";
    case "hoge" => "string hoge";
    case _ => "string unknown";
  };
}

{
  const a: number | null | undefined = 1;
  const r = switch (a) {
    case null => "null";
    case undefined => "undefined";
    case _ => "number";
  };
}

{
  const elem: unknown[] = [];
  const r = switch (elem) {
    case [true] => "[true]";
    case [1] => "[1]";
    case ["hoge"] => "[hoge]";
    case [null] => "[null]";
    case [undefined] => "[undefined]";
    case [elem] => "[elem]";
    case [...tail] => "[...tail]";
    case [x,] => "[x,]";
    case [x, 1] => "[x, 1]";
    case [x, 2, ...tail] => "[x, 2, ...tail]";
    case [{x: 1, y}, [z], ...tail] => "[{x: 1, y}, [z], ...tail]";
  };
}

{
  const obj: Record<string, unknown> = {};
  const r = switch (obj) {
    case { x } => "{ x }";
    case { x: false } => "{ x: false }";
    case { x: 1 } => "{ x: 1 }";
    case { "x": 1 } => "{ 'x': 1 }";
    case { "x": a } => "{ 'x': a }";
    case { ...tail } => "{ ...tail }";
    case { x: [1, 2], y: { a: "a", b } } => "{ x: [1, 2] }";
    case { x: 1, y, ...tail } => "{ x: 1, y, ...tail }";
    case _ if _.x === 1 => "_ if _.x === 1";
    case _ => "_";
  };
}

{
  const a = { x: 1, y: 2, z: 3 };
  const r = switch (a) {
>a : Symbol(a, Decl(SwitchExpression.ts, 75, 20))


> : Symbol((Missing), Decl(SwitchExpression.ts, 0, 0))

  const a: boolean = true;
  const r = switch (a) {
    case true => "yes";
    case _ => "no";
  };
}

{
  const a: boolean = true;
  const r = switch (a) { case true => "yes" case _ => "no" };
}

{
  const a: number = 1;
  const r = switch (a) {
    case 0 => "zero";
    case 1 => "one";
    case _ => "many";
  };
}

{
  const a: string = "hoge";
  const r = switch (a) {
    case "fuga" => "string fuga";
    case "hoge" => "string hoge";
    case _ => "string unknown";
  };
}

{
  const a: number | null | undefined = 1;
  const r = switch (a) {
    case null => "null";
    case undefined => "undefined";
    case _ => "number";
  };
}

{
  const elem: unknown[] = [];
  const r = switch (elem) {
    case [true] => "[true]";
    case [1] => "[1]";
    case ["hoge"] => "[hoge]";
    case [null] => "[null]";
    case [undefined] => "[undefined]";
    case [elem] => "[elem]";
    case [...tail] => "[...tail]";
    case [x,] => "[x,]";
    case [x, 1] => "[x, 1]";
    case [x, 2, ...tail] => "[x, 2, ...tail]";
    case [{x: 1, y}, [z], ...tail] => "[{x: 1, y}, [z], ...tail]";
  };
}

{
  const obj: Record<string, unknown> = {};
  const r = switch (obj) {
    case { x } => "{ x }";
    case { x: false } => "{ x: false }";
    case { x: 1 } => "{ x: 1 }";
    case { "x": 1 } => "{ 'x': 1 }";
    case { "x": a } => "{ 'x': a }";
    case { ...tail } => "{ ...tail }";
    case { x: [1, 2], y: { a: "a", b } } => "{ x: [1, 2] }";
    case { x: 1, y, ...tail } => "{ x: 1, y, ...tail }";
    case _ if _.x === 1 => "_ if _.x === 1";
    case _ => "_";
  };
}

{
  const a = { x: 1, y: 2, z: 3 };
  const r = switch (a) {
>a : Symbol(a, Decl(SwitchExpression.ts, 75, 20))


> : Symbol((Missing), Decl(SwitchExpression.ts, 0, 0))

  const a: boolean = true;
  const r = switch (a) {
    case true => "yes";
    case _ => "no";
  };
}

{
  const a: boolean = true;
  const r = switch (a) { case true => "yes" case _ => "no" };
}

{
  const a: number = 1;
  const r = switch (a) {
    case 0 => "zero";
    case 1 => "one";
    case _ => "many";
  };
}

{
  const a: string = "hoge";
  const r = switch (a) {
    case "fuga" => "string fuga";
    case "hoge" => "string hoge";
    case _ => "string unknown";
  };
}

{
  const a: number | null | undefined = 1;
  const r = switch (a) {
    case null => "null";
    case undefined => "undefined";
    case _ => "number";
  };
}

{
  const elem: unknown[] = [];
  const r = switch (elem) {
    case [true] => "[true]";
    case [1] => "[1]";
    case ["hoge"] => "[hoge]";
    case [null] => "[null]";
    case [undefined] => "[undefined]";
    case [elem] => "[elem]";
    case [...tail] => "[...tail]";
    case [x,] => "[x,]";
    case [x, 1] => "[x, 1]";
    case [x, 2, ...tail] => "[x, 2, ...tail]";
    case [{x: 1, y}, [z], ...tail] => "[{x: 1, y}, [z], ...tail]";
  };
}

{
  const obj: Record<string, unknown> = {};
  const r = switch (obj) {
    case { x } => "{ x }";
    case { x: false } => "{ x: false }";
    case { x: 1 } => "{ x: 1 }";
    case { "x": 1 } => "{ 'x': 1 }";
    case { "x": a } => "{ 'x': a }";
    case { ...tail } => "{ ...tail }";
    case { x: [1, 2], y: { a: "a", b } } => "{ x: [1, 2] }";
    case { x: 1, y, ...tail } => "{ x: 1, y, ...tail }";
    case _ if _.x === 1 => "_ if _.x === 1";
    case _ => "_";
  };
}

{
  const a = { x: 1, y: 2, z: 3 };
  const r = switch (a) {
>a : Symbol(a, Decl(SwitchExpression.ts, 75, 20))
>a : Symbol(a, Decl(SwitchExpression.ts, 75, 20))
>a : Symbol(a, Decl(SwitchExpression.ts, 75, 20))
>a : Symbol(a, Decl(SwitchExpression.ts, 75, 20))
>a : Symbol(a, Decl(SwitchExpression.ts, 75, 20))

    case { x, y, z } if x + y + z > 10 => x * y * z;
>x : Symbol((Missing), Decl(SwitchExpression.ts, 0, 0))
>y : Symbol((Missing), Decl(SwitchExpression.ts, 0, 0))
>z : Symbol((Missing), Decl(SwitchExpression.ts, 0, 0))
>case { x, y, z } if x + y + z > 10 => : Symbol(case { x, y, z } if x + y + z > 10 =>, Decl(SwitchExpression.ts, 75, 24))
>x : Symbol((Missing), Decl(SwitchExpression.ts, 0, 0))
>y : Symbol((Missing), Decl(SwitchExpression.ts, 0, 0))
>z : Symbol((Missing), Decl(SwitchExpression.ts, 0, 0))

    case _ => "10 or less";
>_ : Symbol(_, Decl(SwitchExpression.ts, 77, 9))


>a : Symbol(a, Decl(SwitchExpression.ts, 75, 20))

    case { x, y, z } if x + y + z > 10 => x * y * z;
    case _ => "10 or less";
  };
}
